#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../cross-env/src/bin/cross-env.js" "$@"
else
  exec node  "$basedir/../../../../cross-env/src/bin/cross-env.js" "$@"
fi
# cmd-shim-target=C:/Users/dhobs/Desktop/VISION3D/node_modules/.pnpm/@react-three+drei@9.122.0_@_8da4f3bfd35d3e2e27517936ede3d54c/node_modules/cross-env/src/bin/cross-env.js
